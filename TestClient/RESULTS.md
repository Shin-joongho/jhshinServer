# 부하 테스트 결과

측정일: 2026-09-08

## 환경

| | |
|---|---|
| CPU | Intel i5-13500 (물리 14 / 논리 20) |
| RAM | 64 GB |
| 빌드 | Release x64, MSVC v142, C++20 |
| 서버 설정 | 서비스 IOCP 워커 16, 리슨 1, AcceptEx 사전 게시 128 |
| 풀 | 세션 10,000 / SendBuffer 청크 1,024개 (청크당 40 KB) |
| 네트워크 | **루프백 (127.0.0.1)** — 클라이언트와 서버가 같은 머신 |

## 방법

`TestClient/LoadTest.cpp`

- 연결마다 스레드 1개. 전원 접속 완료 후 **배리어로 동시 출발** (순차 생성 시 계단식 부하가 되어 측정이 왜곡됨)
- 응답을 기다리지 않고 윈도우 W개를 미리 보낸 뒤, `응답 1개 수신 → 1개 추가 송신`으로 in-flight 유지
- 바디 앞 8바이트에 송신 시각을 심어 에코로 돌아온 값으로 왕복 지연 측정
- 클라이언트도 `TCP_NODELAY` 설정
- 측정 구간 10초

"왕복 1회" = 클라이언트 송신 1 + 서버 수신 1 + 서버 송신 1 + 클라이언트 수신 1.

## 1. 연결 수 스윕 (윈도우 8, 바디 64 B)

| 연결 | 왕복/초 | p50 (ms) | p95 (ms) | p99 (ms) |
|---:|---:|---:|---:|---:|
| 1 | 76,644 | 0.102 | 0.128 | 0.158 |
| 4 | 159,863 | 0.199 | 0.246 | 0.297 |
| 16 | 219,710 | 0.453 | 1.524 | 2.787 |
| 50 | 214,230 | 0.473 | 2.357 | 8.601 |
| 100 | 219,716 | 0.447 | 2.391 | 13.439 |
| 200 | 267,882 | 5.583 | 8.114 | 9.070 |
| 500 | 260,734 | 14.676 | 16.315 | 22.197 |

500 연결까지 접속 실패·중간 끊김 0건.

**처리량은 16 연결 부근에서 이미 포화**되고 그 이상은 230k~270k 사이에서 평평하다. 지연은 연결 수에 비례해 증가한다 — 처리량이 고정된 상태에서 in-flight가 늘면 지연이 늘어나는 리틀의 법칙 그대로다. 500 연결 × 윈도우 8 = 4,000 in-flight를 260,000/초로 나누면 15.4 ms로, 실측 p50 14.7 ms와 일치한다.

## 2. 병목이 클라이언트인가 — in-flight 고정, 스레드 수만 변경

| 설정 | in-flight | 왕복/초 |
|---|---:|---:|
| 50 연결 × 윈도우 64 | 3,200 | 233,550 |
| 100 × 32 | 3,200 | 249,820 |
| 200 × 16 | 3,200 | 238,535 |
| 500 × 8 | 4,000 | 264,521 |

클라이언트 스레드 수를 10배 바꿔도 처리량이 평평하다. **클라이언트 스레드 오버헤드가 상한을 만든 것은 아니다.**

## 3. 병목의 성격 — 패킷 크기 변경 (200 연결 × 윈도우 16)

| 바디 | 왕복/초 | 단방향 MB/s |
|---:|---:|---:|
| 64 B | 237,461 | 15.4 |
| 256 B | 239,959 | 59.5 |
| 1,024 B | 253,707 | 248.7 |
| 2,048 B | 212,888 | 416.6 |

**바디를 32배 키워도 초당 왕복 수가 거의 변하지 않는다.** 처리량 상한이 바이트가 아니라 **패킷 개수**에 걸려 있다는 뜻이다. 대역폭(416 MB/s까지 선형 증가)은 한계가 아니다.

패킷당 약 4 µs가 소요되고 있으며, 후보는 다음과 같다.

1. **`ServiceManager::MakeSendPacket`의 전역 뮤텍스** — 모든 세션의 모든 송신이 `m_LastSendBuffer` 하나를 공유하므로 락 하나로 직렬화된다. 가장 유력하다.
2. 세션별 송신 큐 락 (`SessionData::m_SendLock`)
3. `WSARecv` / `WSASend` 시스템 콜 — 다만 송신은 이미 WSABUF 배열로 배칭되어 있다

1번이 맞다면 **SendBuffer 청크를 thread_local 또는 Room 단위로 소유**하도록 바꿨을 때 개선되어야 한다. 다음 측정 대상.

## 4. 반복 변동 (200 연결 × 윈도우 16 × 64 B, 5회)

| 회차 | 왕복/초 | p50 | p95 | p99 | 최대 |
|---:|---:|---:|---:|---:|---:|
| 1 | 254,006 | 1.633 | 10.597 | 44.177 | 1,697 |
| 2 | 238,273 | 0.827 | 4.703 | 747.804 | 10,335 |
| 3 | 290,732 | 9.777 | 14.670 | 25.598 | 11,661 |
| 4 | 267,919 | 9.102 | 14.443 | 41.289 | 2,032 |
| 5 | 235,241 | 0.845 | 4.801 | 725.500 | 1,627 |

처리량 변동은 235k~291k (±12%).

## 측정의 한계 — 이 수치로 주장할 수 없는 것

- **클라이언트와 서버가 같은 머신이다.** 클라이언트 스레드 200~500개가 서버 IOCP 워커 16개와 논리 프로세서 20개를 나눠 쓴다. 서버 단독 상한은 이보다 높을 수 있다.
- **루프백이라 실제 NIC·네트워크 스택 비용이 빠져 있다.**
- **꼬리 지연(p99·최대)은 서버 지연으로 해석할 수 없다.** 지연은 클라이언트 유저 공간에서 응답을 처리한 시점에 측정되므로, 200~500개 스레드가 20코어를 경합하며 생기는 **클라이언트 측 스케줄링 지연이 포함**되어 있다. 최대 10초는 그 오염으로 보는 편이 타당하다. p50까지는 신뢰할 만하다.

신뢰할 수 있는 결론은 두 가지다.

1. 이 환경에서 **초당 약 25만 왕복**이 상한이고 500 연결까지 안정적으로 유지된다.
2. 상한은 **대역폭이 아니라 패킷당 고정 비용**에서 온다.

---

# 병목 추적 (2026-09-08 추가)

측정 조건은 모두 200 연결 × 윈도우 16 × 64 B × 10초, 각 3회 중앙값.

## 5. 워커 스레드 스케일링

| 워커 | 왕복/초 | 1워커 대비 |
|---:|---:|---:|
| 1 | 202,678 | 1.00× |
| 2 | 286,759 | 1.41× |
| 4 | 300,287 | 1.48× |
| 8 | 299,881 | 1.48× |
| 16 | 292,029 | 1.44× |

**워커 2개에서 포화한다.** 논리 프로세서 20개 머신에서 워커를 16배 늘려도 1.5배를 못 넘는다. 암달의 법칙으로 역산하면 직렬 구간이 전체의 약 2/3이다.

## 6. 가설: 전역 SendBuffer 락 — 기각

`ServiceManager::MakeSendPacket`이 모든 세션의 모든 송신에 대해 `m_SendLock` 하나를 잡고 `m_LastSendBuffer`를 공유하는 구조였다. 이것이 직렬 구간이라고 보고, 청크를 `thread_local`로 옮겨 락을 제거했다.

청크 40,960 B ÷ 패킷 68 B ≈ 청크당 600 패킷이므로, 락 획득은 250,000/초 → 약 415/초로 줄어든다.

| 워커 | 변경 전 | 변경 후 | 차이 |
|---:|---:|---:|---:|
| 1 | 202,678 | 207,776 | +2.5% |
| 2 | 286,759 | 296,964 | +3.6% |
| 4 | 300,287 | 298,603 | −0.6% |
| 8 | 299,881 | 302,169 | +0.8% |
| 16 | 292,029 | 294,425 | +0.8% |

**전 구간 노이즈 범위(±3%). 가설 기각.** 전역 락은 병목이 아니었다.

변경 자체는 유지했다. 전역 락이 사라져 구조가 단순해지고, 이후 Room 단위 소유로 옮길 때 그대로 이어지기 때문이다. 다만 **이 환경에서 처리량 개선은 없었다.**

## 7. 가설: 클라이언트 프로세스 한계 — 기각

클라이언트가 왕복 1회당 시스템 콜을 3번(헤더 recv, 바디 recv, send) 부르므로 300k 왕복/초면 초당 90만 콜이다. 서버(왕복당 2회)보다 많아 클라이언트가 천장일 가능성을 검증했다.

| 구성 | 왕복/초 |
|---|---:|
| 클라 1개 × 200 연결 | 303,201 |
| 클라 2개 × 100 연결 | 155,820 + 156,003 = **311,823** |

프로세스를 둘로 나눠도 합계가 같다. **클라이언트 프로세스 단위의 한계가 아니다.**

## 결론

배제된 원인:

- 대역폭 — 패킷을 32배 키워도 왕복/초 불변 (3절)
- 클라이언트 스레드 수 — in-flight 고정 후 스레드 10배 변경해도 불변 (2절)
- 전역 SendBuffer 락 — 제거해도 불변 (6절)
- 클라이언트 프로세스 — 2개로 분산해도 합계 불변 (7절)

남는 설명은 **단일 머신 루프백에서 패킷당 커널 경로 비용**이다. 왕복 1회에 클라이언트 3 + 서버 2 = 시스템 콜 5회와 루프백 통과 2회가 발생하며, 300,000 왕복/초는 왕복당 약 3.3 µs에 해당한다.

즉 **이 측정에서 본 것은 서버의 한계가 아니라 측정 환경의 한계다.** 서버 자체 상한을 재려면 클라이언트를 별도 머신에 두고 실제 NIC를 거쳐야 한다.

---

# 8. CPU 귀속 — 병목 확정

서버에 모니터 스레드를 넣어 `GetProcessTimes`로 자기 CPU 사용량을 2초마다 출력하게 하고, 클라이언트에도 같은 계측을 추가했다.

**200 연결 × 윈도우 16 × 64 B, 302,050 왕복/초 구간:**

| 프로세스 | CPU | 비중 | 왕복/초/코어 |
|---|---:|---:|---:|
| 서버 (IOCP 워커 8) | 2.47 / 20 코어 | 12.3% | **122,300** |
| 클라이언트 (스레드 200) | 16.17 / 20 코어 | 80.8% | **18,700** |

합계 18.6 / 20 코어 = 머신의 93%로 사실상 포화. **그 부하의 대부분을 측정 도구가 만들고 있었다.**

서버는 코어 2.5개로 30만 왕복을 처리하며 17.5개를 놀리고 있다. **서버는 CPU 바운드가 아니라 대기 바운드다.** 앞서 6절에서 락을 제거해도 처리량이 변하지 않은 이유가 여기 있다 — 서버 내부의 어떤 최적화도 의미가 없는 상태였다.

## 효율 비교 — 같은 일에 6.5배

동일 머신, 동일 워크로드에서 코어당 처리량이 6.5배 차이난다.

- **서버**: IOCP 비동기 완료 모델, 워커 8개가 연결 200개를 담당
- **클라이언트**: 스레드-퍼-커넥션 블로킹 모델, 연결마다 스레드 1개

클라이언트는 패킷마다 블로킹 `recv`에서 깨어나며 컨텍스트 스위치가 발생하고, 왕복당 시스템 콜을 3회(헤더 recv, 바디 recv, send) 호출한다. 서버는 완료 통지를 워커 8개가 나눠 처리하고 송신은 WSABUF 배열로 배칭한다.

**IOCP를 쓰는 이유가 이 표에 그대로 나타난다.**

## 부수 확인

- `session 328/10000` — 접속 200 + AcceptEx 사전 게시 128. 20초 부하 내내 정확히 유지되어 **세션 누수 없음**을 확인했다. (Send 완료 실패 경로 수정이 유효)
- `chunk 13/1024` — 청크는 13~15개만 동시 사용된다. 1,024개는 과다하고 64개면 충분하다.

## 다음 단계

서버의 실제 상한을 재려면 클라이언트가 서버보다 가벼워야 한다. 둘 중 하나가 필요하다.

1. 클라이언트를 IOCP 또는 논블로킹 다중화로 재작성
2. 클라이언트를 별도 물리 머신에 두고 실제 NIC 경유

현재 수치는 **"서버가 12.3% CPU로 30만 왕복/초를 처리했다"**까지만 주장할 수 있고, 상한은 아직 보지 못했다.

---

# 9. 경량 클라이언트 — 서버의 실제 처리량

8절에서 병목이 측정 도구로 확인되어, 클라이언트를 다시 만들었다. (`TestClient/LoadTestFast.cpp`)

| | 기존 `LoadTest` | 경량 `LoadTestFast` |
|---|---|---|
| 동시성 모델 | 연결마다 스레드 1개 (블로킹) | 스레드 N개가 여러 연결을 논블로킹 + `WSAPoll` |
| 수신 | 왕복당 `recv` 2회 (헤더 / 바디) | 큰 버퍼로 `recv` 1회 후 여러 패킷 파싱 |
| 송신 | 왕복당 `send` 1회 | 여러 패킷을 모아 `send` 1회 |

수신 패킷은 `_type == PacketType_Server`, `_size == bodySize`를 매건 검증한다. 아래 모든 측정에서 불일치 0건, 중간 끊김 0건.

## 윈도우 스윕 (200 연결, 스레드 8, 바디 64 B)

| 윈도우 | 왕복/초 | 서버 CPU | 클라 CPU | 서버 왕복/초/코어 | 편도 MB/s | p50 (ms) |
|---:|---:|---:|---:|---:|---:|---:|
| 16 | 1,206,166 | 3.81 | 3.80 | 316,579 | 78 | 2.452 |
| 32 | 2,358,542 | 4.91 | 4.21 | 480,355 | 153 | 2.558 |
| 64 | 4,469,457 | 4.05 | 3.86 | 1,103,570 | 290 | 2.787 |
| 128 | 7,670,962 | 3.85 | 3.29 | 1,992,458 | 497 | 3.232 |
| 256 | **8,974,453** | 4.32 | 3.51 | 2,077,420 | 582 | 5.320 |
| 512 | 8,957,330 | 3.96 | 3.37 | 2,261,952 | 581 | 10.628 |

윈도우 256에서 포화한다. 512는 처리량이 같고 p50만 2배가 된다 — in-flight만 늘고 처리량은 고정이라 리틀의 법칙대로 지연만 증가한다.

## 최고 기록

| 설정 | 왕복/초 | 서버 CPU | 클라 CPU | p50 |
|---|---:|---:|---:|---:|
| 100 연결 × 창 256 × 스레드 4 | **9,731,192** | 3.32 / 20 | 1.14 / 20 | 2.463 ms |
| 200 연결 × 창 256 × 스레드 8 | 9,219,026 | 4.44 / 20 | 3.53 / 20 | 5.702 ms |

## 8절 대비

| | 기존 클라 | 경량 클라 | 배수 |
|---|---:|---:|---:|
| 처리량 | 302,050 | 9,731,192 | **32×** |
| 서버 CPU | 2.47 코어 | 3.32 코어 | 1.3× |
| 클라 CPU | 16.17 코어 | 1.14 코어 | **1/14** |

**서버는 처음부터 병목이 아니었다.** 측정 도구가 머신의 80%를 쓰면서 서버 능력의 3%밖에 부하를 걸지 못하고 있었다.

## 배칭이 지배적이다

윈도우 16 → 256으로 늘리면 처리량이 7.4배가 되는데 서버 CPU는 그대로다. 코어당 처리량이 316,579 → 2,077,420으로 **6.6배** 오른다.

윈도우가 커지면 한 번의 완료 통지에 여러 패킷이 함께 도착하고(`SessionData::Recv`의 `while( DivideBuffer() )` 루프가 여러 번 돈다), 송신도 큐에 쌓인 여러 패킷이 WSABUF 배열 하나로 나간다. **IOCP 완료와 시스템 콜 비용이 패킷 수로 나눠진다.**

서버의 `RecvBuffer`가 `PACKET_SIZE`(4,096 B)이므로 한 완료당 최대 약 60개(68 B 기준)까지 뭉칠 수 있다. 이 값이 배치 상한을 정하므로 다음 튜닝 후보다.

## 남은 한계

포화 지점에서도 서버 4.4 + 클라 3.5 = 약 8 / 20 코어로 **머신의 60%가 놀고 있다.** 즉 이 천장도 CPU가 아니다. 9.7M 왕복/초는 편도 582 MB/s, 양방향 약 1.3 GB/s로 루프백 데이터 경로 쪽 한계로 보인다.

또한 스레드 16개 설정에서 `GetProcessTimes` 값이 비현실적으로 낮게 나왔다(클라 0.29코어). Windows의 CPU 시간 계정이 타이머 틱 기반이라, 짧게 실행하고 자주 블로킹하는 스레드가 많아지면 과소 계상된다. **스레드 4~8 설정의 CPU 수치만 신뢰한다.**

여전히 단일 머신 · 루프백이고, 서버가 하는 일은 에코뿐이라 게임 로직 비용은 포함돼 있지 않다.

---

# 10. 패킷 디스패치 도입 (2026-09-09)

## 변경 내용

- `PacketStruct.h`가 프로토콜 정의(`PacketType` enum, `PacketID`, 패킷 구조체)를 담고, X-매크로로 enum과 핸들러 등록을 한 목록에서 생성
- `PacketHandler` — 함수 포인터 배열 기반 디스패치. `Dispatch`에서 타입 범위와 C2S 대역을 검증한 뒤 핸들러 호출, 실패 시 세션 종료
- `SessionData::Recv`가 직접 에코하던 것을 `PacketHandler::Dispatch` 호출로 교체
- **테스트 클라이언트 2개가 서버의 `PacketStruct.h`를 직접 include**하도록 전환. 규격을 두 곳에 두던 것을 한 곳으로 통합
- 바디가 `sizeof( Client_ECHO_Req )` = 100 B 고정으로 변경 (이전 64 B)

## 정확성

`TestClient` 4/4 통과. 단일 왕복 / 여러 패킷 뭉침 분리 / 한 패킷 분할 수신 / 2000회 왕복.
응답 타입이 `PacketType_SERVER_ECHO`(100)로 정상 수신됨.

부하 구간 전체에서 **패킷 불일치 0건, 중간 끊김 0건, 서버 에러 로그 0건**.

## 부하 (경량 클라, 바디 100 B)

| 설정 | 왕복/초 | 서버 CPU | 클라 CPU | p50 |
|---|---:|---:|---:|---:|
| 200 연결 × 창 16 | 1,042,809 | 3.88 | 3.95 | 2.38 ms |
| 200 × 창 128 | 5,054,033 | 3.80 | 3.83 | 4.00 ms |
| 200 × 창 256 | 5,160,255 | 3.88 | 3.67 | 8.31 ms |
| 100 × 창 256 (스레드 4) | **5,535,281** | 3.93 | 3.81 | 3.79 ms |

## 9절과 직접 비교하면 안 된다

**변수가 둘 바뀌었다.** 디스패치 도입과 바디 크기 변경(64 → 100 B)이 동시에 일어났다.

| | 왕복/초 | 편도 MB/s | 서버 CPU |
|---|---:|---:|---:|
| 9절 (직접 에코, 64 B) | 9,731,192 | 661 | 3.32 |
| 10절 (디스패치, 100 B) | 5,535,281 | 575 | 3.93 |

왕복 수는 43% 줄었지만 **바이트 처리량은 661 → 575 MB/s로 거의 같다.** 9절에서 확인한 데이터 경로 천장이 582 MB/s였으므로, 왕복 수 감소는 대부분 패킷이 68 → 104 B로 커진 결과다. 서버 CPU도 같은 범위(3.3~3.9 코어)에 머문다.

**디스패치로 인한 유의미한 비용은 관측되지 않았다.** 다만 정확한 수치는 이 데이터로 말할 수 없다 — 비교 실험은 한 번에 하나만 바꿔야 한다는 걸 놓쳤다.

## 남은 의심 — 핸들러의 중간 복사

```cpp
memcpy_s( &echoReq, sizeof( echoReq ), bufferData, bufferSize );   // 수신버퍼 -> echoReq
MakeSendPacket( ..., (char*)&echoReq, sizeof( echoReq ) );          // echoReq -> 청크
```

`MakeSendPacket`이 청크로 복사하므로 앞의 복사는 순수 추가 비용이다. 초당 550만 패킷 × 100 B = **약 550 MB/s의 여분 memcpy**이며, 바이트 처리량 13% 차이의 유력한 원인이다.

실제 게임 패킷은 구조체로 역직렬화해 필드를 읽어야 하므로 이 패턴이 맞지만, 에코는 받은 버퍼를 그대로 넘기면 된다. 다음 측정 대상.

---

# 11. 디스패치 비용 재측정 — 변수를 분리하다

10절에서 왕복 수가 43% 줄어 보였으나, 디스패치 도입과 바디 크기 변경(64 → 100 B)이 동시에 일어나 원인을 가릴 수 없었다. 두 가지를 되돌려 **변수를 하나만 남겼다.**

- `Client_ECHO_Req`를 64 B로 되돌림 → 9절과 동일한 68 B 패킷
- 에코 핸들러의 중간 복사 제거 → `bufferData`를 `MakeSendPacket`에 직접 전달

이제 9절과의 유일한 차이는 **패킷 디스패치 경유 여부**다.

## 윈도우 스윕 (200 연결, 스레드 8, 바디 64 B)

| 윈도우 | 9절 (직접 에코) | 11절 (디스패치) | 차이 |
|---:|---:|---:|---:|
| 16 | 1,206,166 | 1,208,673 | +0.2% |
| 32 | 2,358,542 | 2,370,540 | +0.5% |
| 64 | 4,469,457 | 4,555,837 | +1.9% |
| 128 | 7,670,962 | 7,569,362 | −1.3% |
| 256 | 8,974,453 | 8,760,121 | −2.4% |
| 512 | 8,957,330 | 8,543,162 | −4.6% |

윈도우 128·256은 3회 측정 중앙값. 나머지는 단일 측정.

## 최고 기록 비교

| | 9절 | 11절 (3회 중앙값) | 차이 |
|---|---:|---:|---:|
| 100 연결 × 창 256 × 스레드 4 | 9,731,192 | 9,401,478 | −3.4% |
| 서버 CPU | 3.32 | 4.05 | — |

서버 CPU는 측정 간 변동이 커서(같은 설정에서 3.3~5.3 코어) 이 정밀도로는 비교가 어렵다. 스윕 전체의 중앙값으로 보면 9절 약 4.0, 11절 약 3.85로 사실상 동일하다.

## 결론

**디스패치 도입 비용은 측정 오차 이하다.** 처리량 차이가 −4.6% ~ +1.9%로, 앞서 확인한 반복 변동폭(±3~12%) 안에 들어간다.

배열 인덱싱 1회와 함수 포인터 간접 호출 1회는 패킷당 수 나노초 수준이므로 예상과 일치한다.

**10절에서 관측된 26% 효율 저하는 디스패치가 아니라 다음 둘 때문이었다.**

1. **패킷이 68 → 104 B로 커짐 (1.53배)** — 데이터 경로 천장(약 580 MiB/s)에 걸린 상태였으므로 패킷이 커진 만큼 왕복 수가 줄었다. 9,401,478 ÷ 1.53 = 6.14M 으로, 10절 실측 5.5M과 대체로 일치한다.
2. 핸들러가 수신 버퍼를 `Client_ECHO_Req`에 복사한 뒤 그것을 다시 청크로 복사 — 여분의 memcpy

둘을 되돌리자 9절 수준으로 복귀했다.

**다만 2번의 기여는 거의 없었다.** 12절에서 `memcpy` 비용을 따로 측정한 결과, 100 B 복사는 약 1 ns이므로 초당 550만 회여도 코어 하나의 0.55% 수준이다. **1번이 사실상 전부였다.**

처음에는 "여분 memcpy가 초당 550 MB"라는 크기에 눈이 갔지만, 절대 시간으로 환산해 보기 전에는 그 숫자가 큰지 작은지 알 수 없었다.

## 배운 것

비교 실험에서 변수를 둘 이상 동시에 바꾸면 **결과를 해석할 수 없다.** 10절은 "디스패치 비용이 관측되지 않았다"는 잘못된 결론으로 이어질 뻔했고, 실제로는 반대 방향(26% 저하)의 수치를 보고 있었다. 변수를 하나씩 되돌려 재측정한 뒤에야 두 원인이 분리되었다.

측정 도구를 만들 때(9절)도, 측정 조건을 바꿀 때(11절)도 같은 실수를 반복했다는 점을 기록해 둔다.

---

# 12. `memcpy` vs `memcpy_s` 마이크로벤치마크

11절에서 "여분 memcpy"를 원인 후보로 지목했으므로, 실제 비용을 따로 쟀다.

## 조건

MSVC v142, Release `/O2`, x64. 3천만 회 반복. 64개 슬롯을 순환해 L1에 상주하는 최선 조건이며, 최적화 제거를 막기 위해 매 회 목적지 첫 바이트를 체크섬에 더한다.

## 결과 (호출당 평균)

| 크기 | `memcpy` | `memcpy_s` | 배수 | 절대 차이 |
|---:|---:|---:|---:|---:|
| 64 B | 0.60 ns | 0.78 ns | 1.30× | +0.18 ns |
| 128 B | 1.13 ns | 1.17 ns | 1.04× | +0.04 ns |
| 256 B | 2.24 ns | 2.26 ns | 1.01× | +0.02 ns |
| 1,024 B | 16.95 ns | 17.93 ns | 1.06× | +0.98 ns |
| 4,096 B | 68.86 ns | 69.72 ns | 1.01× | +0.86 ns |

`memcpy_s`의 추가 비용은 **호출당 0.2 ns 안팎의 고정값**이다. 64 B에서 30%로 보이는 것은 분모가 작기 때문이다.

초당 940만 패킷 기준으로 환산하면 9.4M × 0.18 ns = 1.7 ms/초, 즉 **코어 하나의 0.17%**다. 측정 가능한 수준이 아니다.

## 그래서 `memcpy_s`를 서버에 쓸 것인가

속도는 판단 근거가 못 된다. 실제로 고려할 것은 의미론이다.

**① 제약 위반 시 기본 동작이 프로세스 종료다.** MSVC의 `_s` 계열은 invalid parameter handler를 호출하고, 기본 핸들러는 프로세스를 끝낸다. 잘못된 패킷 하나로 서버 전체가 내려갈 수 있다. 사용한다면 `_set_invalid_parameter_handler`로 로깅 후 계속 진행하도록 바꿔야 한다.

**② 실패 시 목적지를 0으로 채운다.** MSVC 구현은 `destSize < count`이면 `memset( dst, 0, destSize )`를 먼저 실행한다. 부분 복사가 아니라 대상 버퍼 파괴다.

**③ 막아주는 것은 목적지뿐이다.** 소스 버퍼의 크기는 알지 못하므로, 네트워크 코드에서 정작 위험한 "수신 버퍼에 그만큼의 데이터가 실제로 있는가"는 보장하지 못한다. 그것은 `RecvBuffer::DivideBuffer`가 책임지는 영역이다.

**④ Annex K는 C11의 선택적 부록**이라 glibc는 구현하지 않는다. Linux 포팅 시 전부 걷어내야 한다.

**결론.** 크기를 이미 명시적으로 검증했다면 `memcpy`로 충분하다. 검증하지 않았다면 `memcpy_s`에 맡기는 대신 `if`로 직접 검사하는 편이 낫다 — 실패 시 동작(세션 종료 / 무시 / 로깅)을 직접 정할 수 있고 의도가 코드에 드러난다.

현재 에코 핸들러는 `if( bufferSize != sizeof( echoReq ) ) return false;`로 이미 검증하고 수신 버퍼를 그대로 `MakeSendPacket`에 넘긴다.

---

# 13. Room / Broadcast 경로 (2026-09-10)

잡큐와 룸이 들어오면서 새로 생긴 경로를 측정했다. 1~12절은 전부 ECHO(1:1) 경로이고, 이 절은 브로드캐스트(1:N) 경로다.

```
CLIENT_ENTER     -> RoomManager::PushJobByRooms -> 룸 스레드 -> Room::Enter
                 -> SERVER_ENTER + Server_Enter_Ack(roomID) 회신
CLIENT_BROADCAST -> 룸 스레드 -> Room::BroadCast
                 -> 같은 룸의 자신을 제외한 모든 세션에게 1개씩
```

## 측정 환경이 다르다 — 1~12절과 직접 비교 금지

**이 절의 측정은 논리 프로세서 6개 머신에서 했다.** 1~12절은 i5-13500(논리 20)이었다. 숫자를 가로질러 비교하면 안 된다.

| | 1~12절 | 13절 |
|---|---|---|
| 논리 프로세서 | 20 | **6** |
| 빌드 | Release x64 | Release x64, MSVC v143, C++20 |
| 네트워크 | 루프백 | 루프백 |

## 테스트 클라이언트 — `LoadTestRoom.cpp`

`LoadTestFast`는 왕복(request → response)을 센다. 브로드캐스트는 1 요청이 N-1 응답이 되므로 왕복 개념이 없다. 그래서 별도 클라이언트를 만들었다.

- 송신은 **연결당 초당 rate 개**로 페이싱하고, 유입(보낸 브로드캐스트)과 유출(받은 메시지)을 따로 센다
- 서버가 룸을 무작위 배정하므로 ENTER ack 에서 roomID 를 받아 기록한다. 기대 유출은 룸별로 계산해야 한다
  - 기대 유출 = Σ(룸 r 에서 보낸 수 × (룸 r 의 멤버 수 − 1))
- 지연은 **편도**로 잰다. 모든 연결이 한 프로세스 안에 있어 `steady_clock` 이 공유되므로, "A 가 보낸 시각"과 "B 가 받은 시각"의 차가 그대로 편도 지연이다
- 수신 패킷은 매건 `_type == SERVER_BROADCAST`, `_size == 64` 를 검증한다

`verify` 모드로 정확성을 따로 검사한다. 접속 → ENTER → 룸 분포 확인 → 같은 룸 전달 → 송신자 본인 제외 → **다른 룸으로 누출 없음** → 연속 100건 프레이밍.

## 정확성

최종 코드에서 **6/6 통과**. 부하 5회를 돌린 뒤 다시 돌려도 6/6이다. 부하 구간 전체에서 패킷 불일치 0건, 중간 끊김 0건.

---

## 발견 1 — 끊긴 세션이 룸에 남는다

전달률이 100%를 넘는 것을 보고 찾았다. 서버를 새로 띄운 직후부터 **같은 설정**을 반복하면:

| 회차 | 유출/초 | 전달률 |
|---:|---:|---:|
| 1 | 12,473 | **100.00%** |
| 2 | 17,074 | 145.24% |
| 3 | 25,732 | 217.40% |
| 4 | 22,046 | 185.07% |

1회차가 정확히 100.00%이므로 기대 유출 계산식은 맞다. 부하가 같은데 유출만 늘어난다. 이 상태에서 격리 검사를 돌리면 실패한다.

```
4) [실패] 송신자 본인에게도 브로드캐스트가 돌아왔다
```

**경로.**

1. `Room::Leave` 는 `CLIENT_LEAVE` 패킷으로만 호출됐다. 클라이언트가 그냥 끊으면 `m_roomUser` 엔트리가 영구히 남는다
2. 세션 풀도 반환되지 않았다 (모니터: 128 → 238 → 280 → 291). 그래서 죽은 `SessionData` 객체가 옛 `m_Socket` 값을 든 채 살아 있다
3. Windows 는 SOCKET 핸들 값을 재사용한다. 새 연결이 그 번호를 받는다
4. 그 룸에서 브로드캐스트가 일어나면 죽은 세션의 `InsertSendQueue` → `WSASend(m_Socket)` 이 **무관한 살아있는 연결로 나간다**

`Room::BroadCast` 의 `session.second == broadSession` 은 `SessionData` 포인터 비교라, 소켓 번호가 같아도 다른 객체면 걸러내지 못한다.

**수정.** 모든 종료 경로(`IOCP::Worker` 의 recv/send 실패, `RecvObject::Execute` 의 0바이트 및 `Recv` 실패)가 `CloseSession` 직접 호출 대신 세션의 룸으로 `Job_Leave` 를 밀도록 바꿨다.

**수정 후.** 같은 서버 인스턴스에서 5회 연속:

| 회차 | 유출/초 | 전달률 | p50 |
|---:|---:|---:|---:|
| 1 | 9,634 | 100.00% | 0.166 ms |
| 2 | 10,513 | 100.00% | 0.170 ms |
| 3 | 10,144 | 100.00% | 0.191 ms |
| 4 | 9,996 | 100.00% | 0.172 ms |
| 5 | 10,355 | 100.00% | 0.177 ms |

세션 풀은 128 ↔ 178 로 정확히 복귀한다. 부하 후 격리 검사 6/6.

---

## 발견 2 — 룸 ID 를 못 받은 세션은 아무것도 반환되지 않는다

발견 1의 수정이 만든 회귀다. `PushJobByRooms( job_leave, session->GetRoomID() )` 인데, ENTER 를 하지 않은 세션은 `GetRoomID()` 가 −1 이다. `if( 0 > index ) return;` 가드에 걸려 **잡이 조용히 버려지고, `Room::Leave` 도 `CloseSession` 도 실행되지 않았다.**

ENTER 없이 50개 접속했다 끊는 프로브로 확인:

| | 수정 전 | 수정 후 |
|---|---|---|
| 세션 풀 | 128 → 178 → **178 고정** | 128 → 178 → **128 복귀** |
| `conn` | 50에서 안 내려감 | 0 복귀 |
| 소켓 | **CLOSE_WAIT 50개** | **0개** |

세션 엔트리뿐 아니라 **소켓까지 샜다.** 헬스체크, 포트 스캔, 핸드셰이크 도중 끊기는 클라이언트가 전부 이 경로다.

**수정.** `PushJobByRooms` 가 인덱스 범위 밖이면 `CloseSession( jobObject->GetSession() )` 을 직접 부른다.

---

## 발견 3 — 송신 실패 경로만 룸에서 지우지 않았다

발견 1의 수정이 종료 경로 전부에 적용되지 않았다. `CloseSession` 호출부를 전수 조사해서 찾았다.

| 위치 | 경로 | 룸 제거 |
|---|---|---|
| `IOCP.cpp:93` | `RecvStart` 실패 | `Job_Leave` ✓ |
| `IOCP.cpp:108` | 송신 타입 완료 실패 | `Job_Leave` ✓ |
| `IOCP.cpp:198` | 수신 0바이트 | `Job_Leave` ✓ |
| `IOCP.cpp:220` | `Recv` 실패 | `Job_Leave` ✓ |
| `IOCP.cpp:247` | `SendObject::Execute` 송신 실패 | **`CloseSession` 직접 호출 ✗** |

소켓은 닫히고 세션 풀에도 반환되지만 `m_roomUser` 에는 엔트리가 남는다. 그다음은 발견 1과 동일하다.

**측정에서 이걸 못 잡은 이유가 중요하다.** 부하 클라이언트가 항상 정상 종료(FIN)해서 **수신 경로로만** 끊겼기 때문이다. 이 경로는 송신 도중 상대가 사라질 때(RST, 강제 종료, 네트워크 단절) 탄다. 즉 **테스트가 도달하지 못한 코드였다.**

**재현 프로브.** 정상 종료로는 탈 수 없으므로 별도로 만들었다.

1. 접속 + ENTER 로 룸에 입장
2. 브로드캐스트만 계속 보내고 **수신은 전혀 하지 않는다** → 서버의 송신 큐와 소켓 송신 버퍼가 차서 `WSASend` 가 pending 으로 쌓인다
3. `SO_LINGER {1, 0}` 으로 `closesocket` → FIN 이 아니라 **RST** 를 보낸다 → 서버의 pending `WSASend` 가 실패로 완료된다

**수정.** `IOCP.cpp:247` 도 다른 네 곳과 같이 `Job_Leave` 를 게시한다. `GetRoomID()` 가 −1 이어도 `PushJobByRooms` 가 발견 2의 경로로 넘겨주므로 안전하다.

추가로 `SessionData` 에 `atomic<bool> m_Connected` 가 들어가, `Room::Enter` 는 중복 입장을, `Room::Leave` 는 중복 퇴장을 막는다.

**검증.** 30 연결 × RST 폭풍 5회(총 150건, 전부 송신 적체 상태에서 강제 종료) 후:

| 항목 | 결과 |
|---|---|
| 세션 풀 | 128 로 복귀 |
| 소켓 | Listen 1개 외 0 |
| 서버 에러 로그 | 0건 |
| 이어서 정상 부하 3회 전달률 | 100.00% / 100.00% / 100.00% |
| 룸 격리 검사 | 6/6 |

**세션 풀이 128 로 복귀한 것 자체가 룸 제거의 증거다.** `Room::m_roomUser` 는 `SessionDataRef`(shared_ptr)를 들고 있으므로, 룸에 엔트리가 남아 있으면 참조 계수가 0 이 되지 않아 풀 반환이 일어날 수 없다.

**남은 구멍 (측정하지 않음).** `PushJobByRooms` 의 범위 밖 경로가 부르는 `CloseSession` 에는 중복 방지 검사가 없다. 수신 경로의 `Job_Leave` 가 먼저 처리되어 `SetRoomID( -1 )` 을 하고 난 뒤 송신 경로의 `Job_Leave` 가 만들어지면, 그쪽은 −1 을 들고 이 경로로 빠져 `closesocket()` 이 두 번 불린다. 위 RST 폭풍에서는 관측되지 않았으나 창이 좁을 뿐 닫힌 것은 아니다. 종료 1회 전이 플래그를 `CloseSession` 안으로 옮기면 모든 경로가 한 지점에서 막힌다.

---

## 처리량

세 가지 구성을 순서대로 쟀다. 연결 50 고정, 바디 64 B, 10초.

**(A) 전원 룸 1 — `PushJobByRooms( job_enter, 1 )` 하드코딩. 룸 스레드 1개가 전량 처리**

| rate | 유출/초 | 전달률 | p50 |
|---:|---:|---:|---:|
| 10 | 24,530 | 100% | 0.43 ms |
| 20 | 49,764 | 100% | 0.64 ms |
| 25 | 61,263 | 100% | 3.24 ms |
| 30 | 74,209 | 100% | 89.7 ms |
| 35 | 65,397 | **76%** | 983 ms |
| 60 | 81,467 | **55%** | 2,119 ms |

**(B) `rand() % 4` — 룸 스레드 4개. 매 측정마다 서버 재시작**

| rate | 유출/초 | 전달률 | p50 | 서버 CPU |
|---:|---:|---:|---:|---:|
| 20 | 11,715 | 100.00% | 0.19 ms | 0.01/6 |
| 50 | 28,989 | 100.00% | 0.27 ms | 0.09/6 |
| 100 | 57,767 | 99.98% | 0.31 ms | 0.16/6 |
| 150 | 89,347 | 99.99% | 0.37 ms | 0.93/6 |
| 200 | 120,994 | 99.54% | 0.37 ms | 0.44/6 |
| 300 | 145,503 | **82.03%** | 312 ms | 0.43/6 |

**(C) `rand() % 5` + 누수 수정 — 룸 스레드 5개**

| rate | 유출/초 | 전달률 | p50 | 클라 CPU |
|---:|---:|---:|---:|---:|
| 20 | 9,315 | 100.00% | 0.17 ms | 0.01/6 |
| 50 | 24,789 | 100.00% | 0.22 ms | 0.01/6 |
| 100 | 49,565 | 100.00% | 0.31 ms | 0.07/6 |
| 200 | **104,347** | 100.00% | 1.07 ms | 0.35/6 |
| 300 | 154,706 | 99.98% | 3.3 ms | 0.59/6 |
| 400 | 190,035 | 98.65% | 7.6 ms | 1.15/6 |
| 500 | 228,437 | 99.62% | 43 ms | 1.40/6 |
| 650 | 321,429 | 99.77% | 17 ms | 1.47/6 |
| 800 | 385,482 | 99.86% | 37 ms | 1.60/6 |

서버 CPU 최대 1.97/6.

**동작 성격이 바뀌었다.** (A)와 (B)는 천장을 넘으면 전달률이 무너졌는데, (C)는 385k 까지 99.8%를 유지하고 지연만 늘어난다. 백로그가 쌓이되 결국 다 나간다.

실용 기준으로 **(C) 에서 유출 약 104,000/초까지 100% 전달에 p50 1.07 ms** 다.

**세 구성을 가로질러 비교하면 안 된다.** 룸 수, 브로드캐스트당 팬아웃 크기(49 / ~11.5 / ~9), 누수 수정이 동시에 바뀌었다. 어느 것이 얼마나 기여했는지 이 데이터로는 분리할 수 없다. 10절에서 한 실수와 같은 종류다.

rate 400 이상은 클라 CPU(1.15~1.60)가 서버(1.97)와 같은 자릿수라 8절과 같은 오염 구간이다. **그 구간은 서버 한계로 주장하지 않는다.**

## ECHO 경로와의 대조

같은 머신, 같은 서버, 같은 클라이언트 모델, 연결 50:

| 경로 | 루프백 통과 | 처리량 | p50 |
|---|---:|---:|---:|
| ECHO (왕복, IOCP 워커가 직접 처리) | 2회 | 661,169 왕복/초 | 0.499 ms |
| BROADCAST (잡큐 → 룸 스레드) | 1회 | 위 표 참조 | 0.17~1.07 ms |

**지연은 루프백 탓이 아니다.** 루프백을 두 번 통과하는 ECHO 가 더 빠르다. 두 경로의 유일한 차이는 잡큐를 거쳐 룸 스레드로 넘어가는지 여부다.

## 팬아웃 크기가 메시지 수보다 지배적이다

(B) 구성에서 **유출량이 같은데 지연이 660배 차이난다.**

| 설정 | 브로드캐스트당 팬아웃 | 유출/초 | p50 |
|---|---:|---:|---:|
| 50 연결 × rate 200 | ~11 | 120,994 | **0.37 ms** |
| 100 연결 × rate 50 | ~24 | 121,676 | **244 ms** |

`Room::BroadCast` 가 룸 멤버를 한 스레드에서 동기로 순회하므로, 룸이 커지면 순회 뒤쪽 멤버가 그만큼 밀린다. **초당 메시지 수보다 룸 크기가 먼저 걸린다.**

---

## 측정 도구가 만든 착시 — p50 16 ms

처음 측정에서 p50 16 ms 가 나왔고, **부하를 5배 늘려도 16 ms 로 일정했다.** 큐잉이면 부하에 따라 늘어야 하므로 고정 지연으로 보고 원인을 찾았다.

원인은 클라이언트였다. 연결마다 송신 위상을 흩으려던 코드가

```cpp
c.nextSendAt = startNs + ( interval * c.id ) % interval;   // 항상 0
```

`interval * id` 는 언제나 `interval` 의 배수라 나머지가 0이다. 위상 분산이 전혀 안 되고 50개 연결이 동시에 쐈다. 룸 스레드 하나가 50 × 49 = 2,450 건을 직렬 처리하는 큐잉이 그대로 지연으로 잡혔다. 버스트 크기가 rate 와 무관하니 지연도 일정했던 것이다.

고치자 **p50 16.0 ms → 0.692 ms**. 처리량은 변하지 않았다.

## 기각한 가설 둘

16 ms 를 쫓으며 세운 가설을 측정으로 기각했다. 둘 다 틀렸다.

**① Windows 타이머 분해능.** p50 16 / p95 30 / 최대 58 ms 가 기본 타이머 틱 15.6 ms 의 배수로 보였다. `timeBeginPeriod(1)` 을 넣었으나 **지연이 그대로였다.** 이후 폴 루프 반복 횟수를 직접 계측하니 초당 12,446회(반복당 0.08 ms)로, 애초에 병목이 아니었다.

**② TCP_NODELAY 미설정.** `SetOptions` 가 `bool`(1바이트)로 `setsockopt` 을 부르는데 TCP_NODELAY 는 `BOOL`(4바이트)을 기대하므로 실패할 것으로 봤다. 프로브로 확인한 결과 **optlen=1 로도 성공하고 값이 1로 읽힌다.** 리슨 소켓에만 걸지만 `SO_UPDATE_ACCEPT_CONTEXT` 후 accept 소켓도 1을 상속한다. 둘 다 정상이다.

**배운 것.** 8절·11절과 같은 실수를 또 했다. 부하와 무관하게 일정한 값은 큐잉이 아니라는 신호였는데, 서버 쪽 가설부터 세웠다. 측정 도구를 먼저 의심했어야 했다.

---

## `PakcetHandler Error` 는 패킷 문제가 아니다

고부하(rate 800) 구간에서 서버 로그에 이 메시지가 간헐적으로 찍힌다(3회 중 1회, 4건). 위치가 **정확히 런 경계** — 마지막 `conn 50` 모니터 줄과 `conn 0` 줄 사이다.

`SessionData::Recv` 는 마지막이 `return RecvStart();` 다. 즉 **패킷 처리 실패뿐 아니라 `WSARecv` 재등록 실패에도 false 가 나온다.** `RecvObject::Execute` 는 그 false 를 전부 "PakcetHandler Error" 로 찍는다. 클라이언트가 끊을 때 FIN 도착과 recv 재등록이 경합하면 나는 **정상 종료 경로**다.

클라이언트가 중간 끊김 0으로 보고한 것, 패킷 불일치가 전 구간 0인 것, 고부하에서만 나는 것이 모두 이 설명과 맞는다. 다만 어느 분기가 탔는지 계측해 확인하지는 않았다. **코드와 타이밍 상관에 따른 추론이다.**

메시지를 분리하면 진단이 쉬워진다.

---

## 코드에서 본 것 — 측정하지 않음

**① ~~`SessionData::Send()` 에 패킷이 갇히는 창~~ — 철회. 틀린 지적이었다.**

아래 두 블록 사이에서 락이 풀리는 것을 보고, 그 사이 `InsertSendQueue` 가 끼어들면 패킷이 큐에 남은 채 아무도 보내지 않는 상태가 된다고 적었다.

```cpp
{ lock; 큐를 m_Send 로 전부 옮김 }              // 여기서 락을 놓는다
{ lock; if( m_Send.Empty() ) { m_SendFlag = false; return; } }
```

**성립하지 않는다.** 이 시나리오는 `Send()` 가 큐가 빈 상태로 진입해야 하는데, 그럴 수 없다. `m_SendFlag` 가 송신자 자격을 한 명에게만 주는 토큰 역할을 한다.

- `InsertSendQueue` 는 락 안에서 push 한 뒤 **플래그가 false 였을 때만** 진입한다 (false → true 전이를 이긴 스레드 하나)
- `CheckSendComplete` 는 **큐가 비어있지 않을 때만** 진입하고, 플래그를 true 로 유지한다
- 완료 통지는 세션당 `SendObject` 가 하나라 동시에 두 개가 뜨지 않는다

따라서 `Send()` 에는 항상 한 스레드만 있고, 진입 시점의 큐는 반드시 비어있지 않다. `if( m_Send.Empty() )` 는 도달 불가능한 방어 코드다.

**락이 풀리는 구간만 보고 진입 조건을 확인하지 않은 것이 실수였다.** 경합을 지적할 때 "락이 풀린다"는 근거가 되지 못한다. "그 구간에 실제로 두 스레드가 들어올 수 있는가"를 먼저 봐야 한다.

**② `Job_Leave` 중복** — 부분 수정됨. 아래 「발견 3」 참고.

**③ `PushJobByRooms` 의 부작용 범위.** 인덱스가 범위 밖이면 잡 종류와 무관하게 세션을 닫는다. BROADCAST 핸들러도 `GetRoomID()` 를 넘기므로 **ENTER 전에 BROADCAST 를 보내면 끊긴다.** 프로토콜 위반 처리로 의도한 것이면 문제없다.

**④ `Room::BroadCast` 의 `for( auto session : m_roomUser )`** 가 `auto&` 가 아니라 멤버마다 `shared_ptr` 를 복사한다. 초당 38만 메시지면 원자적 refcount 가 그만큼 돈다.

**⑤ 룸 분포가 균등하지 않다.** 16 연결에서 `room0=9 room1=18 room2=12 room3=11`, 다른 런에서는 한 룸이 0이었다. MSVC `rand()` 는 스레드마다 독립 상태를 갖고 전부 시드 1로 시작하므로 IOCP 워커들이 같은 수열을 낸다 — 그럴듯한 원인이지만 분리해 측정하지 않았다.

## 남은 것

**종료가 되지 않는다.** `JobQueue::Stop()` 은 호출부가 없고, `RoomManager::Work` 의 `while(true)` 가 `StartJob()` 을 감싸고 있어 stop 으로 리턴해도 다시 들어간다. `Room::LeaveAll()` 은 비어 있다.

**고부하 반복 시 전달률 하락.** rate 800 을 같은 서버에서 3회 돌리니 99.11% → 93.37% → 77.33% 로 떨어졌다. 세션·청크 수치는 정상 복귀하는데 원인을 모른다. 클라 CPU 가 서버와 비슷한 오염 구간이라 측정 쪽 문제일 수도 있어, 이것만으로 서버 문제라고 말하지 않는다.

**검사하지 않은 영역.** 동시 접속 폭주, 기형 패킷(길이 필드 조작), 장시간 구동, 메모리 증가 추이, `ObjectPool` 고갈 시 동작.

## 이 절에서 주장할 수 있는 것

1. 룸 격리, 세션 수명, 소켓 반환, 프레이밍은 검사한 범위에서 정상이다
2. 6코어 루프백에서 **유출 약 104,000 메시지/초까지 100% 전달, p50 1.07 ms**
3. 브로드캐스트 경로의 병목은 CPU 가 아니라 **룸당 단일 스레드의 동기 순회**다. 초당 메시지 수보다 룸 크기가 먼저 걸린다
4. 발견 1·2는 재현·수정·재검증까지 마쳤다

**주장할 수 없는 것.** 세 구성 간 처리량 차이의 원인 분해, rate 400 이상 구간의 서버 한계, 위 "코드에서 본 것" 다섯 항목의 실제 발생 여부.
